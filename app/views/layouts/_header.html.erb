<!-- ↓hederのclassの中身はbooststrapのclass
    navbar:ナビゲーションバーを表示　navbar-fixed-top:ナビバーを上部に固定する　
    navbar-inverse:ナビバーの色をを反転（黒色）する　-->
<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Attendance App", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "ホーム", root_path %></li>
        <% if logged_in? %>
          <li><%= link_to "全てのユーザー", users_path %></li>
          <li class="dropdown"> <!-- dropdown(dropdown-toggleも)はドロップダウンリストを作成するbootstrap -->
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><!-- aタグ：リンクを貼る -->
              メニュー <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "勤怠", current_user %></li><!-- link_to "Show",user_path(current_user)と同じ -->
              <li><%= link_to "設定", edit_user_path(current_user) %></li>
              <li class="divider"></li><!-- divider(bootstrap)意味:区切りをいれる -->
              <li>
                <!-- ↓link_to で method:delete　と指定してる訳→link_toは通常指定しなければ
              　　methodは get なので指定している　※form_tagは指定しなければ post -->
                <%= link_to "ログアウト", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "ログイン", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>